<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Compras" parent="Base.Theme.Compras">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@color/tema</item>
        <item name="android:statusBarColor">@color/tema</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
    </style>

    <style name="Base.Theme.Compras" parent="Base.Base.Theme.Compras">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@color/tema</item>
        <item name="android:statusBarColor">@color/tema</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
    </style>
</resources>